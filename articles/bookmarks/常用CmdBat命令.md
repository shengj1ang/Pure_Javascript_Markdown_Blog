2014-03-16
Visibility: Public


```
taskkill /f /im qq.exe（结束qq进程）

rd /s/q "C:\TDDownload"（删除文件夹）

del/f/s/q "C:\qq.exe"（删除文件）

reg delete /f "HKEY_CURRENT_USER\Software\WinRAR"(删除注册项)

xcopy /e "C:\windows\file" "D:\file"（将前者文件夹复制到后者目录内"）

copy /y "C:\abc\kk.exe" "C:\a"（将前者文件复制到后者目录内"）

move /y "C:\abc\kk.exe" "C:\a"（将前者文件/目录 移动到后者目录内")

move /y "C:\abc\kk.exe" "C:\abc\QQ.exe"（将前者文件重命名为后者)

md "C:\aaa" (创建文件夹)
```